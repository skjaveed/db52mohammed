extends layout

block content
  h1= title
  p Hey this is Index.pug

  input(type="button" class="btn btn-primary" value="Press Me" onClick="update()")
  div(id = "target") Display
  script.
      var client_count=0
      function update(){
        client_count += 1;
        document.getElementById('target').innerHTML = "Value now : " + client_count
      };
